inspecode:

  checkstyle:
    #condif-file: buildSrc/src/main/resources/checkstyle.xml  # not work with `options:`
    options:
      -c: buildSrc/src/main/resources/checkstyle.xml
      -p: inspecode-checkstyle.properties
    auto-fix: true   # Failed, caused by `-p` is not effective

  pmd:
    auto-fix: true
